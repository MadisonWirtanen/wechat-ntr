# Contributing

感谢你对 wechat-ntr 的改进。

这个项目的核心目标是：**保持极低部署门槛，同时提供一个清晰、轻量、无历史记录的临时聊天室。**

## 开发原则

提交改动时，请尽量保持以下约束：

1. **单文件可部署**  
   `index.html` 应继续能够作为完整静态应用直接部署。

2. **不要要求应用后端**  
   当前项目依赖公共 MQTT 服务，但不需要自建 API、数据库或用户系统。若提议新增后端，请说明为什么值得改变这一定位。

3. **保护不可信输入**  
   用户昵称、消息、文件名等内容应通过 DOM API / `textContent` 渲染，不要直接拼接到 `innerHTML`。

4. **保持协议兼容**  
   当前文本协议的关键约定包括：
   - 本地 `YYYYMMDDHH` roomKey
   - `chat/MD5(roomKey)` topic
   - CryptoJS AES passphrase
   - JSON 消息类型：`message` / `join` / `leave`
   - 附件使用 `<topic>/files/v1`

   如果必须改变这些约定，请考虑旧客户端兼容或明确增加协议版本。

5. **不要把“已发送”误写成“已送达”**  
   MQTT QoS 0 和本地 publish 成功并不能证明其他客户端已经收到。附件的 SHA-256 ACK 只表示对应接收端完成了文件校验。

6. **附件必须保持原始字节**  
   不要为了预览而修改原文件。预览与原文件下载应保持分离，且只有完整性校验通过后才开放接收文件下载。

## 本地测试

项目没有构建步骤。可以直接打开 `index.html`，或使用静态 HTTP Server：

```bash
python -m http.server 8080
```

建议至少进行以下手工回归：

- 两个浏览器标签页在同一小时加入后可以互相收发文字。
- Enter 发送、Shift + Enter 换行正常。
- 中文输入法组合输入时不会误发送。
- 断网后发送按钮被禁用，恢复网络后能够重连。
- 页面不在远离底部时强制滚动。
- 昵称、消息和文件名中的 HTML 字符不会被当作 HTML 执行。
- 发送 0 B、小文件、恰好 5 MiB、超过 5 MiB 和超过 30 MiB 的文件，确认大文件仅提示而不被拦截。
- 页面附件总量超过 30 MiB 时仍可发送和接收；内存分配失败时显示可重试提示。
- 接收后的 SHA-256 与原文件一致。
- 断开附件传输后可以继续接收缺失分块。
- PNG / JPEG / GIF / WebP 的预览不影响原文件下载。
- 退出聊天室后消息和附件对象被清理。

## Pull Request 建议

PR 描述中建议包含：

- 改了什么
- 为什么要改
- 是否影响文本消息协议
- 是否影响附件协议
- 是否改变 5 MiB 建议大小、8 KiB 分块或 40 个附件项等约定
- 手工测试了哪些场景
- 如有 UI 变化，附桌面端和移动端截图

## 提交范围

尽量让一个 PR 只解决一个主题。协议、安全或附件传输相关改动应避免夹带无关的大规模格式化，以便审查。


# Security Policy

## 项目定位

wechat-ntr 是一个纯前端、基于公共 MQTT Broker 的临时聊天室实验项目。

当前实现包含 AES 加密和 SHA-256 文件完整性校验，但它**不是经过正式安全审计的端到端加密通信产品**，也不应被用于高敏感度通信。

## 当前安全模型

### 消息

加入房间时，客户端根据本地时间生成：

```text
roomKey = YYYYMMDDHH
topic   = chat/MD5(roomKey)
```

消息 JSON 使用 CryptoJS AES 和该 `roomKey` 作为 passphrase 加密后，通过公共 MQTT Broker 发送。

这意味着：

- 密钥空间可预测，知道协议的人可以计算当前或其他小时的 roomKey。
- topic 同样可由时间值推导。
- 没有账号、成员认证或发送者身份签名。
- 昵称和 senderId 不构成可信身份。
- 不能把“消息被 AES 加密”理解为“房间只有预期参与者能够访问”。

### 附件

附件保持原始字节并按块传输。接收端在完整接收后计算 SHA-256，并与发送端公布的摘要比对。

SHA-256 在这里用于检测传输后的字节是否一致；它不解决参与者身份认证问题。

### 公共基础设施

当前客户端连接：

```text
wss://broker.emqx.io:8084/mqtt
```

这是第三方公共服务。本项目无法保证其长期可用性、容量、日志策略、限流规则或其他运营行为。

## 不建议传输的内容

请勿使用本项目传输：

- 密码、API Key、私钥、恢复码
- 身份证件或其他高敏感个人资料
- 银行、支付和财务信息
- 医疗隐私资料
- 商业机密
- 任何要求强身份认证、前向保密或经过审计的机密通信内容

## 报告安全问题

如果发现的问题可能导致用户数据暴露、跨房间读取、代码执行、文件完整性绕过或其他实际安全影响，请尽量避免在公开 Issue 中直接发布可立即利用的完整细节。

如果仓库启用了 GitHub Private Vulnerability Reporting，请优先使用 **Security → Report a vulnerability**。

如果没有可用的私密报告入口，可以先创建一个不包含漏洞利用细节的 Issue，说明希望与维护者建立私密联系方式。

普通的安全建议、威胁模型讨论或不涉及可利用细节的问题，可以直接使用 GitHub Issues。

## 支持范围

项目目前没有正式版本发布周期或长期支持分支。安全修复以默认分支 `main` 的最新代码为准。
